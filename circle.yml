machine:
  python:
    version: 2.7.5
dependencies:
  pre:
    - make install-dependencies
    - make install-wkhtmltopdf
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) make test-sqlite ;; 1) make test-postgres ;; esac:
        parallel: True
  post:
    - make test-flake8
env:
  UPS_LICENSE_NO=CCDB15CA7B1B4C36
  UPS_USER_ID=cappy123abc
  UPS_PASSWORD=cC3jA03T
  UPS_SHIPPER_NO=V19170
